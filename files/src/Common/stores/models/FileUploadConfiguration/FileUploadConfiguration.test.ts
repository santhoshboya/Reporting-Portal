// import fileUploadConfig from '../../../fixtures/fileUploadConfig.json'

import FileUploadConfiguration from '.'

describe('FileUploadConfiguration', () => {
   it('should intiate values', () => {
      // const fileUploadConfiguration = new FileUploadConfiguration(
      //    fileUploadConfig
      // )
      // expect(fileUploadConfiguration.accessKeyId).toBe(
      //    fileUploadConfig.aws_access_key_id
      // )
      // expect(fileUploadConfiguration.awsSessionToken).toBe(
      //    fileUploadConfig.aws_session_token
      // )
      // expect(fileUploadConfiguration.secretAccessKey).toBe(
      //    fileUploadConfig.secret_access_key
      // )
   })
})
