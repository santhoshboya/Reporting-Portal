const BASE_URL = 'https://nkb-backend-beta.apigateway.in/api/'

const endpoints = {
   loginOrRegister: 'login_or_register/v1/',
   getFileUploadConfig: `${BASE_URL}s3_uploader/credentials/v1/`
}

export default endpoints
