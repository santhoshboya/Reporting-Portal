import styled from '@emotion/styled'
import tw from 'tailwind.macro'
const InputElement=styled.input`${tw``}
  height: 40px;
  width:320px
  border-radius: 2px;
  border: solid 1px var(--light-blue-grey);
  background-color: var(--white);`
export {InputElement}