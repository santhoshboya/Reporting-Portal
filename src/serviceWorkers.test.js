import { register } from './serviceWorker'

describe('Service workers test', () => {
   it('should test register', () => {
      const config = {
         onUpdate: () => {},
         onSuccess: () => {}
      }
   })
})
